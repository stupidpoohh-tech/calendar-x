# 캘린더X

할 일과 아이디어와 돈을 같은 날짜 위에서 관리하는 캘린더 웹앱.
상용 캘린더의 기능 과잉을 덜어내고, 실제로 쓰는 동작만 남긴다.
이전 이름은 Dada Calendar. Firebase 프로젝트 ID(`dada-calendar-524ec`)와 백업 파일의
`app` 문자열은 호환을 위해 옛 이름을 유지한다.

## 이 앱이 답하려는 질문

1. 오늘 무엇을 해야 하는가
2. 떠오른 것을 어디에 적어 두는가
3. **다음 입금까지 이 돈으로 며칠 버티는가**

3번이 차별점이다. 투두이스트·노션·뱅크샐러드를 각각 이기려 하면 이길 수 없지만,
세 축이 하나의 타임라인을 공유하는 제품은 드물다.

원래는 "이 달 말 예상 잔고" 로 답했는데, 예측이 잘 맞지 않는 것을 사용자가 여러 번
경험했다. 그래서 형제 앱 tide-over 의 원칙을 흡수했다 — **예측하지 않는다.**
표시하는 숫자는 "예상 잔고" 가 아니라 "이 날까지 쓸 수 있는 한도" 다.

## 구조 — 하나의 타임라인, 네 개의 렌즈

**탭은 컬렉션이 아니라 필터다.** 모든 항목은 `entry` 하나이고 `kind` 로만 갈린다.

| 렌즈 | kind | 용도 |
|------|------|------|
| 전체 | (없음) | 세 축을 한 화면에. 오늘 패널 + 통합 캘린더 |
| TODO | `task` | 할 일. 기간·중요·긴급·상태 |
| 💡 | `idea` | 아이디어 단문 |
| 가계부 | `money` | 예정 입출금 + 잔고 + 대출 + 현금흐름 |

축 간 이동은 `kind` 필드 하나를 바꾸는 일이다 (`convertKind`).
아이디어 → 할 일 승격이 이 구조의 핵심 이점이다.

**같이 보기는 다섯 번째 렌즈가 아니다.** TODO 의 하위 기능이고, 진입점은 TODO 화면
안쪽의 조용한 한 줄이다 (`SharedBar`). 렌즈 칸을 하나 더 만들면 축이 다섯 개라는 뜻이
되고, 큰 세그먼트 토글(`내 TODO | 같이 보기`)을 상시 노출하면 1차 네비게이션과 위계가
겹친다. 열면 본문 자리를 대신 차지하고, 상단 렌즈를 누르면 닫힌다.

## 기술 스택

- Vite + React 18 + TypeScript (strict, `noUncheckedIndexedAccess`)
- Firebase Auth (이메일/비밀번호) + Firestore
- 배포: Cloudflare Pages. Firebase config 는 `VITE_FIREBASE_*` 환경변수
- 테스트: vitest (도메인) + `@firebase/rules-unit-testing` (보안 규칙)

## 파일 구조

```
src/domain/     순수 함수만. React 를 import 하지 않는다
  types.ts        도메인 타입
  constants.ts    COLORS, LENSES, MONEY_TYPES, STATUSES
  date.ts         날짜 유틸. 시간대 의존은 todayISO() 하나뿐
  entry.ts        생성 · 파생 필드 · kind 변환
  recurrence.ts   반복 전개 (저장하지 않는 가상 항목)
  tide.ts         "며칠 버티나" 계산. tide-over 흡수. 한도·정산·요약
  budget.ts       생활비 예산 · 세이브. 한도에서 미리 빠져 있는 몫
  recovery.ts     회복. 간격·빚·회차 생성. 순수 상태 전이만 담는다
  money.ts        최소 단위 정수 <-> 표시 문자열
  filters.ts      렌즈 · 필터
  shared.ts       같이 보기. 겹쳐 보기(source ⊕ overrides) · 초대 코드.
                  **Entry 를 만들어 돌려주는 함수가 하나도 없다**
  dday.ts         D-Day. 남은 날은 저장하지 않고 볼 때마다 센다
src/data/       Firestore 접근
  firebase.ts     초기화. 오프라인 지속성 ON
  repo.ts         구독 · 쓰기 · 배치
  converters.ts   문서 <-> 도메인 (읽기는 전부 방어적으로)
  backup.ts       내보내기 (파일 형식 · 버전)
  backupFile.ts   파일 읽기. **변환 전에** 원시 JSON 을 검증한다
  restore.ts      복원 검증 · 실행 순서. 전체 교체는 꺼져 있다
  migrate.ts      이관 전 items -> 신규 스키마
  sharedRepo.ts   공유 보드 구독 · 쓰기 · 초대 · 맞추기.
                  개인 경로에 **쓰는** 함수가 없다
  sharedConverters.ts  공유 문서 <-> 도메인. 갱신 쓰기는 overrides · hidden 을 담지 않는다
src/ui/         화면 컴포넌트 (App: 렌즈 화면, TideBar: 며칠 버티나 카드,
                BudgetPanel: 생활비 · 세이브. 카드 안쪽 접힌 줄,
                balanceEditor: 잔고 편집 상태 + 조각. 카드가 소유한다,
                Recovery*: 회복 상세 · 밀린 회복 줄 · 설정 그룹 ·
                OFF 항목 목록 관리(상세와 설정이 같은 조각을 쓴다),
                Shared*: 같이 보기 진입점 · 화면 · 항목 편집 · 고정메모 · D-Day · 초대,
                ime.ts: 조합 중 Enter 판정 한 줄. 입력칸마다 다시 발명하지 않는다)
src/pages/Tide/ /tide 잔고캘린더. tide-over 저장소의 앱을 화면째로 옮겨 왔다.
                원본 화면을 유지한 독립 앱. 캘린더X 코드를 런타임에 쓰지 않는다
src/app/        셸과 상태 훅
src/styles/     tokens.css (디자인 토큰) + app.css (전 컴포넌트 스타일)
firestore.rules 보안 규칙
```

## Firestore 데이터 구조

```
users/{uid}/entries/{id}    일정 · 아이디어 · 지출 통합
users/{uid}/accounts/{id}   잔고 (계좌 단위)
users/{uid}/debts/{id}      대출 1건 = 1문서
users/{uid}/budgets/{id}    생활비 예산 1건 = 1문서 (기간 + 총액)
users/{uid}/reserves/{id}   세이브 1건 = 1문서 (날짜 없음)
users/{uid}/pins/{id}       고정 메모
users/{uid}/items/{id}      이관 전 구조. 읽기 전용
users/{uid}                 문서 자체. migratedAt + recovery (회복 규칙 1개)

sharedBoards/{boardId}              같이 보기 보드. ownerUid · memberUids · memberNames
sharedBoards/{boardId}/items/{id}   공유 TODO. **문서 id = 원본 entry 의 id**
sharedBoards/{boardId}/pins/{id}    공유 고정메모 (id 는 'memo' 하나로 고정)
sharedBoards/{boardId}/ddays/{id}   공유 D-Day
sharedInvites/{code}                초대장. 문서 id 가 곧 초대 코드다
```

**공유 자료는 개인 경로 밖에 둔다.** `users/{uid}` 아래에 두면 TODO 하나를 보여 주려고
그 사용자 영역을 열어야 하고, 그러면 아이디어 · 가계부 · 대출 · 잔고 · 회복까지 함께
열린다. 같이 보기를 넣으면서 `users/{uid}` 규칙은 **한 글자도 느슨해지지 않았다**.

**구독은 세 갈래다** (`useStore`).

| 갈래 | 범위 | 쓰임 |
|------|------|------|
| 화면용 | 보고 있는 달 ±1 | 캘린더·리스트에 그릴 항목 |
| 계산용 | **오늘** −1 ~ +14달 | 한도·다음 입금일·정산 |
| 반복 | 전량 | 월 조회로 잡히지 않는다 |

예산과 세이브는 컬렉션이 작고 달과 무관해 전량 구독한다.

**effect 는 셋으로 나눠 둔다.** 한 effect 에 묶으면 달을 넘길 때마다 계산 구독까지 끊고
다시 붙는다 — 커서와 무관한 값인데도 재구독이 돌고, 그때마다 빈 목록이 흘러 머리 숫자가
깜빡인다. 지금은 커서를 옮기면 화면용 구독만 다시 붙는다.

**계산 창은 뒤로 한 달뿐이다.** 잔고 기준일이 그보다 오래됐다면 그 사이의 **비반복**
입출금이 목록에 없다 (반복은 전량 구독하므로 빠지지 않는다). 그 상태의 정산 차액은
실제보다 크다. `settle()` 이 `complete: false` 로 알리고, 화면은 확정 금액 대신
"무엇이 빠졌는지" 를 말한 뒤 잔고만 갱신한다.

`entry` 의 파생 필드는 **손으로 채우지 않는다.** 항상 `withDerived()` 를 거친다.

- `ymSpan: YearMonth[]` — 항목이 걸친 모든 달. `array-contains-any` 로 월 단위 조회
- `isRecurring: boolean` — `recurrence` 에서 파생

## 지켜야 할 것

**한글 IME.** Enter 핸들러에는 반드시 `e.nativeEvent.isComposing` 체크가 있어야 한다.
없으면 조합 중 Enter 가 두 번 발화해 입력이 사라진다. 판정은 `ui/ime.ts` 한 곳에 있다
(브라우저마다 알려 주는 방식이 달라 `key === 'Process'` · `keyCode 229` 도 함께 본다).

**날짜·시각 칸은 아무 데나 눌러도 열린다** (`ui/pickerField.ts` 의 `PICKER`).
`<input type="date">` 의 기본 동작은 오른쪽 끝 달력 그림을 눌러야만 선택기가 열리는
것이다. 칸 가운데를 누르면 `2026` 같은 조각에 커서만 들어가고 아무 일도 일어나지
않는다 — 칸 전체가 버튼처럼 생겼는데 실제로 눌리는 자리는 16px짜리 그림 하나뿐이라,
누른 사람은 "안 눌린다" 고 읽는다. 모바일에서는 더 심하다.
`showPicker()` 는 지원하지 않거나 이미 열려 있으면 던지므로 삼키되, **여기서만**
삼킨다 — 실패해도 그림을 눌러 여는 기본 동작이 그대로 남기 때문이다.
새 날짜·시각 칸에는 `{...PICKER}` 를 붙인다. `src/pages/Tide/` 는 별개 앱이라 이
규칙 밖이다.

**날짜는 문자열.** 전부 `'YYYY-MM-DD'` / `'HH:mm'` 벽시계 값이다. UTC 인스턴트로
저장하지 않는다. 8월 20일 14시 회의는 어디서 보든 8월 20일 14시다.
패딩 없는 값(`2026-5-3`)이 섞이면 `normalizeDate()` 로 맞춘다.

**순간에서 날짜를 뽑을 때는 `localDateOf()` 를 쓴다.** `Date#toISOString().slice(0, 10)`
은 UTC 로 환산한 날짜라, 한국 시간 오전 9시 이전이면 하루가 밀린다. 테스트는
`vite.config.ts` 가 `TZ=Asia/Seoul` 로 못박아 돌린다 — UTC 기기에서는 이 부류의
결함이 아예 드러나지 않기 때문이다.

**오늘은 최초 렌더에 고정되지 않는다.** `useToday()` 가 자정 타이머 ·
`visibilitychange` · `focus` 세 갈래로 다시 잰다. 이 값 하나가 계산 창(`tideWindow`) ·
한도 · 정산 기준 · 오늘 카드를 전부 정하므로, 하루가 밀리면 화면 전체가 조용히 어제를
가리킨다. 화면 컴포넌트는 시계를 직접 읽지 않고 `todayISO` prop 으로 받는다.
값이 실제로 달라졌을 때만 상태를 바꾼다 — 리렌더가 나면 구독이 다시 붙는다.

**금액은 최소 단위 정수.** `amountMinor`. KRW 는 원 단위. 실수를 쓰지 않는다.

**"비어 있다" 와 "아직 못 받았다" 는 다른 상태다.** 오프라인 지속성 때문에 첫 스냅샷은
대개 로컬 캐시에서 오고, 캐시가 비어 있으면 **빈 목록**이 온다. 그것을 자료 없음으로
읽으면 한도가 잔고 그대로 떴다가 잠시 뒤 값이 튄다. `useStore` 는 화면용(`display`)과
계산용(`calc`)의 상태를 따로 내놓는다 — `loading` · `cache` · `live` · `error`.

**계산 상태는 다섯 구독을 종합한다.** 금액에 필요한 자료는 **계산용 월 항목 · 반복 항목 ·
잔고 · 생활비 예산 · 세이브** 다섯이다. 뒤의 둘은 한도를 **깎는** 값이라, 아직 안 온 사이에
확정으로 내보내면 실제보다 큰 한도가 떴다가 잠시 뒤 줄어든다. 예전에는 월 구독 하나만 보고 있어서, 2025년에 시작한 반복 지출이 아직
도착하지 않은 사이에 그 지출이 빠진 한도가 확정값처럼 떴다. `combineFeeds()` 가 가장
나쁜 갈래를 따른다 — 하나라도 실패면 error, 하나라도 못 받았으면 loading, 하나라도
캐시에서 왔으면 cache, `empty` 는 셋 다 비었을 때만.

**화면이 쓰는 갈래는 네 개다** (`calcStateOf`). `loading` · `error` · `unconfirmed`
(캐시가 비어 "없음" 을 확정할 수 없다) · `ready`. `ready` 가 아니면 카드는 숫자를 지어내지
않고 이유를 적고, 달력도 셀에 한도를 적지 않는다. `ready` 이면서 캐시 기준이면 **숫자는
내되** 완전성 한계를 한 줄로 덧붙인다 — 오프라인이라고 화면을 통째로 막지 않는다.

**메타데이터 변경까지 구독한다.** `fromCache` 는 자료가 그대로인 채 true → false 로
바뀐다 (캐시로 먼저 그리고, 잠시 뒤 서버가 같은 값을 확인해 준다). 기본 리스너는 문서가
바뀔 때만 발화하므로 그 전환을 못 받아 화면이 영영 "캐시 기준" 으로 남는다. 그래서
`includeMetadataChanges` 를 켜고, 문서 변경이 없는 발화에서는 **앞서 만든 배열을 그대로**
돌려줘 위쪽 useMemo 가 헛돌지 않게 한다.

**조회 범위가 바뀌면 앞 범위의 준비 상태를 물려주지 않는다.** 값에 범위 키를 붙여 두고
렌더에서 키가 어긋나면 버린다. effect 로 비우면 한 번은 어긋난 채로 그려진다.

**계정이 바뀌면 앞 계정의 자료를 한 렌더도 남기지 않는다.** 구독을 끊어도 마지막
스냅샷은 상태에 그대로 남는다. `useStore` 는 렌더 중에 상태를 비우고, `dataUid` 가
따라잡을 때까지 EMPTY 를 내보내며, 콜백마다 소유자 uid 를 확인한다.

**tide 계산은 필터와 무관하다.** 필터로 항목을 가렸다고 한도가 늘어나면 안 된다.
그래서 `MonthCalendar` 는 그리는 목록(`entries`, 필터 적용됨)과 한도를 재는 목록
(`tideEntries`, 필터 이전)을 따로 받는다.

**가계부 렌즈 달력은 날짜마다 한도를 적는다.** 잔고캘린더가 셀마다 적어 주던
"이 날까지 쓸 수 있는 돈"이다 (`limitOn` · `compactAmount`). 오늘 이전에는 적지
않는다 — 지나간 발생분은 한도를 못 건드리므로 잔고만 반복될 뿐이다.

**과거 지출은 입력하지 않는다.** 잔고를 옮겨 적는 순간 그 사이의 변동 지출이
정산된 것으로 본다 (`domain/tide.ts` 의 `settle`). 잔고에는 `asOf`(날짜)와
`checkedAt`(ISO datetime) 을 함께 둔다.

**`asOf` 는 경계, `checkedAt` 은 순서다.** 정산 구간의 시작은 `asOf` 다 — 예정 항목은
`startDate` 만 갖고 시각이 없으므로 정산은 애초에 날짜 단위로만 정확할 수 있다.
`checkedAt` 은 하루에 두 번 적거나 계좌가 여럿일 때 어느 기록이 가장 최근인지 고르는
데만 쓴다. 여기서 날짜를 뽑아 쓰지 않는다.

**자료가 모자라면 확정 차액을 내지 않는다.** 기준일이 계산 구독 구간(`tideFrom`)보다
앞서면 그 사이의 한 번짜리 입출금이 목록에 없다. 그럴듯하게 틀린 숫자를 내느니
무엇이 빠졌는지 말하고 잔고만 갱신한다 (`Settlement.complete` · `coveredFrom`).

**지원 범위는 계좌 여럿 · 통화 하나다.** 한도는 모든 계좌의 잔고 합에서 잰다. 통화는
한 번에 하나만 다룬다 — 환율 변환 기능이 없고, `amountMinor` 는 통화마다 최소 단위가
달라(원 vs 센트) 섞어 더하면 틀린 줄도 모르게 틀린다. 섞이면 `currencyScopeOf()` 가
막고 카드는 숫자 대신 이유를 적는다. **달력 셀도 같이 막는다** — 카드가 거절한 바로 그
숫자를 달력이 지어내면 아무 소용이 없다.

**잔고 편집은 계좌 하나를 대상으로 한다.** 예전에는 합계를 보여 주면서 저장은 첫 계좌에만
했다 — 계좌가 둘이면 적은 금액이 첫 계좌로 통째로 들어가 이중으로 세어졌다. 지금은 고른
계좌의 잔고만 바꾸고, 정산은 "나머지 계좌 합 + 새 금액" 을 새 총액으로 삼는다.

**확정 차액을 못 내는 이유는 세 갈래다** (`Settlement.reason`). `coverage` 는 기준일이
자료 구간보다 앞선 것, `accounts` 는 계좌마다 마지막으로 적은 날이 다른 것, `currency` 는
통화가 섞인 것이다. 셋 다 금액을 지어내지 않고 무엇이 모자란지 말한 뒤 잔고만 갱신한다.

**머리 숫자와 그 아래 목록은 같은 기간 예산을 본다.** `limitOn` 과 `upcomingInHorizon`
이 기간 예산(span)에 거는 조건이 같아야 한다 — 끝점 뒤에 시작하는 기간 예산은 양쪽 모두에서
빠지고, 끝점에 걸치면 양쪽 모두에 남은 몫 전체가 들어간다. 한쪽만 고치면 목록의 합과 머리
숫자가 어긋난다.

**한도의 끝점은 "다음 예정 입금 전날"이다.** 급여일 같은 별도 필드는 없다. 급여도
그냥 예정 입금이고, 주급(7일마다)도 같은 규칙으로 돈다. 다음 입금이 없으면 30일 뒤로
폴백한다. 기간 예산(span)은 흐름이라 "다음 입금" 에서 제외한다 — 안 그러면 끝점이
매일 내일이 된다.

**기간 예산은 표시(총액)와 계산(하루 몫)이 다르다.** 목록에는 총액 한 줄로 보이지만,
계산은 일할 몫이 기준이라 기간 중간에 잔고를 다시 적어도 정산이 어긋나지 않는다.
한도(`limitOn`)만 예외 — 기간에 들어서는 순간 남은 몫 전체를 예약한다.

**한도의 시작 경계는 오늘이 아니라 잔고를 적은 날이다** (`unsettledAfter`).

예전에는 `(오늘, d]` 만 더했다. 그래서 **오늘 날짜로 적은 입출금이 어디에도 반영되지
않았다** — 머리 숫자도, 오늘 셀도, 남은 예정 목록도 그대로였다. 내일로 적으면 바로
반영되는데 오늘만 사라지니 저장이 안 된 것처럼 보였다.

잔고는 그 날의 사실이고, 그 다음에 잡힌 예정은 아직 그 숫자에 들어 있지 않다. 잔고
기준일이 사흘 전이면 그 사흘 사이의 예정도 아직 잔고 밖이므로 함께 센다. 다만 **오늘은
넘지 않는다** — 이 앱은 "과거 지출은 입력하지 않는다" 를 전제로 하므로 적어 둔 항목은
아직 일어나지 않은 계획이고, 오늘 적은 계획도 앞에 있다. 그래서 잔고를 오늘 적었더라도
오늘 것은 한도에서 뺀다.

**정산은 어제까지만 흡수한다.** 한도가 오늘 것을 세므로, 정산까지 흡수하면 같은 돈이
두 번 빠진다. 둘이 겹치지도 사이가 비지도 않는다 — `settle` 은 `(잔고 기준일, 어제]`,
한도는 그 다음부터 `d` 까지다. `limitOn` · `upcomingInHorizon` · `settle` 이 같은 경계를
쓰므로 목록의 합과 머리 숫자가 맞고, 정산 뒤에도 한도가 흔들리지 않는다.

**잔고 기준일 이전 발생분은 한도를 못 건드린다.** 그 값은 이미 잔고에 들어 있다.
그런 항목을 지워도 한도가 변하지 않는 게 정상이다.

**화면용 목록과 계산용 원본은 다른 목록이다.** `materialize()` 는 반복 항목을 화면에
그릴 발생분으로 펼치는데, 펼친 사본에는 원본의 `recurrence` 가 그대로 남는다. 그 목록을
tide 에 넘기면 발생분마다 반복이 **한 번 더** 전개돼 같은 입출금을 여러 번 센다
(주간 1만원 한 달치가 −4만이 아니라 −14만이 된다). 그래서 펼친 사본은 `virtual` 표식을
달고, tide 의 모든 입구가 그것을 보고 `TideInputError` 로 거절한다. `virtual` 은 저장
경로에 없으므로 사용자 데이터로는 켤 수 없다 — 배선 실수만 잡힌다.

**계산 창은 커서가 아니라 오늘을 기준으로 잡는다.** 한도·다음 입금일·정산은 오늘의
함수이지 보고 있는 달의 함수가 아니다. 커서 창으로 계산하면 달력을 넘길 때마다 머리
숫자가 바뀐다. 앞으로 14달인 이유는 `horizonOf` 가 다음 입금을 400일까지 찾기 때문이다 —
창이 그보다 좁으면 실제로 있는 입금을 못 보고 "입금 없음 → 30일 뒤" 로 조용히 물러난다.
달력의 날짜별 한도는 계산 창이 덮는 달에만 적는다. 자료가 없는 달에 0을 더해 그럴듯하게
틀린 값을 내지 않는다.

**전체 교체는 꺼져 있다.** (`data/restore.ts` 의 `safeReplace`)

한때 "먼저 쓰고 나중에 지우면 안전하다" 고 적었다. **틀렸다.** 백업은 기존 id 위에
그대로 쓴다(upsert). 문서 A 를 파일 내용으로 덮어쓴 뒤 문서 B 쓰기가 실패하면, 삭제를
한 번도 부르지 않았어도 A 의 원래 내용은 이미 사라져 있다. 여러 배치는 원자적이지 않고,
클라이언트 SDK 에는 컬렉션을 가로지르는 트랜잭션이 없다. 메모리에 든 원본으로 되돌리는
것도 안전장치가 못 된다 — 탭이 닫히면 그 원본도 사라진다.

그래서 **불완전한 파괴적 기능을 남기지 않는다.** `safeReplace()` 는 `RestoreIO` 를
받지도 않으므로 쓰기·삭제가 새어 나갈 자리가 없다. 안전한 교체(원본을 별도 스냅샷에
먼저 굳히고 그것이 확인된 뒤에만 교체)는 후속 작업이다.

**병합은 실제 쓰기 시점에 존재를 확인한다.** `fetchAll` 로 후보를 좁히는 것은 읽기·쓰기를
줄이기 위한 것일 뿐이다. `fetchAll → 없는 id 고르기 → setDoc` 사이에 다른 탭이 같은 id 를
만들면 덮어쓴다. `createManyIfAbsent()` 가 트랜잭션 안에서 존재 확인과 쓰기를 함께 해
그 창을 닫는다. 이미 있으면 **충돌로 남기고 건드리지 않는다.**

`writeMany` · `createManyIfAbsent` 는 **실패를 던지지 않고 결과로 돌려준다.** 부르는 쪽이
`failed` 를 읽지 않으면 한 건도 저장되지 않아도 "가져왔습니다" 가 뜬다. 실제로 그랬다.
건수는 파일 총계와 맞아떨어져야 한다 — `created + skippedExisting + conflicts + failed`.

**검증은 변환 *전에* 원시 JSON 을 본다.** (`data/backupFile.ts`)
`converters.ts` 가 빠진 값을 오늘 날짜나 기본값으로 메우는 것은 **이미 저장된 문서를
화면에 띄우기 위한** 것이다. 그것을 먼저 돌리고 검증하면 이미 멀쩡해진 값만 본다 —
잘못된 날짜는 오늘로, 소수 금액은 잘려서, id 없는 항목은 사라진 뒤였다.
`readBackupFile()` 이 JSON 파싱 → 구조·버전 검증 → 항목 검증 → 그다음에야 변환한다.
없는 컬렉션을 빈 배열로 넘겨짚지 않는다 — v2·v3 는 네 배열이 모두 필수다.
예전 형식 변환이 제외하거나 자른 항목은 `notes` 로 돌려주고 화면에 보여 준다.

**반복 전개분은 저장하지 않는다.** id 가 `원본id@YYYY-MM-DD` 형태이고,
편집·삭제는 `baseIdOf()` 로 원본을 향한다.

**반복은 원점부터 세지 않고 조회 범위 근처로 건너뛴다.** 2025년에 시작한 매일 반복을
2026년 9월에서 보면 원점부터는 600칸이 넘게 헛돈다. 예전에는 그 헛도는 칸을
`MAX_RECURRENCE_OCCURRENCES`(400)가 먼저 잘라서 오래된 무기한 반복이 캘린더에서
통째로 사라졌다. 상한을 올리는 대신 `firstIndexOnOrAfter()` 로 건너뛰고, 상한은
**화면에 낼 개수**의 상한으로만 남는다. `count` 는 여전히 원점부터 센다.
`domain/tide.ts` 쪽은 처음부터 건너뛰기를 하고 있었다 — 그래서 예전에는 화면에
안 보이는 지출이 한도만 깎는 상태였다.

**글씨 크기는 rem 으로만 적는다.** `app.css` · `base.css` 의 `font-size` 는 전부 rem 이고
(16px 기준), 설정의 '글씨 크기' 가 `html` 의 font-size 한 줄을 바꿔 전부를 함께 키운다.
px 로 적으면 그 규칙만 설정을 무시한다. 여백·아이콘은 px 그대로라 함께 커지지 않으므로
폭은 ±12.5%(14/16/18px)로 묶어 둔다. 기본값 '자동' 은 `html` 에 font-size 를 두지 않는
것이고, 그래야 브라우저·OS 의 글씨 크기 설정을 따른다.
좁은 화면(≤480px)의 상단 줄만 예외다 — 렌즈 네 칸과 로그인 버튼이 한 줄에 들어가야 해서
`min()` 으로 상한을 둔다. `src/pages/Tide/styles.css` 는 별개 앱이라 이 규칙 밖이다.

**렌즈 강조색을 배경으로 쓸 때는 `--on-lens` 를 짝으로 쓴다.** 강조색은 다크에서
밝아지므로 흰 글자를 고정하면 다크 모드에서 흰 배경에 흰 글자가 된다.

**한 렌즈에 카드는 하나다.** 전체 렌즈는 오늘 카드, 가계부 렌즈는 며칠 버티나 카드.
생활비·세이브·대출·고정 메모는 그 카드 **안쪽에** 접힌 줄로 들어간다(`TideBar` 의 children). 카드를
여럿 세우면 모바일에서 캘린더가 화면 두 번 아래로 밀린다.

**금액은 카드에 하나만 적는다.** 잔고를 따로 적지 않는다 — 예정된 입출금이 없는 구간은
한도가 곧 잔고라 같은 숫자가 두 번 뜨고, 남은 날이 하루면 하루 몫까지 같아져 세 번 뜬다.
큰 숫자를 누르면 그 자리에서 잔고를 고치고(`useBalanceEditor`), 그 아래에는 금액 없이
"언제 적은 값인지"(`BalanceNote`)만 남긴다. 하루 몫은 남은 날이 2일 이상일 때만 적는다.

**떠 있는 표면은 `--bg-elev`.** 다크에서 `--bg`(#0a0a0a)는 페이지 `--bg-soft`(#141414)보다
어두워서, 모달에 쓰면 배경에 잠겨 투명하게 보인다.

## 생활비 예산과 세이브 (Budget · Reserve)

**MoneyType 은 흐름만 말한다.** 예전에는 종류 하나가 흐름·예산·확보·결과를 전부 지고
있었다 (`income` · `expense` · `repay` · `priority` · `living` · `save` · `free`).
지금은 역할이 넷으로 갈려 있다.

| 역할 | 어디에 | 값 |
|------|--------|-----|
| 흐름 | `money.type` | `income` · `expense` — 새 입력은 이 둘뿐 |
| 예산 | `budgets` 컬렉션 | 기간 + 총액. 지출이 `money.budgetId` 로 가리킨다 |
| 확보 | `reserves` 컬렉션 | 날짜 없는 금액 |
| 결과 | `limitOn` | 가용 한도. **입력할 수 없다** |

**생활비는 기간형 지출이 아니라 주머니다.** 예전에는 `sign: -1` 인 기간형 지출이라,
9월 생활비 70만을 적고 점심 1만 2천을 적으면 **71만 2천**이 나가는 것으로 세어졌다.
예산은 나가는 돈의 상한인데 항목이 하나 더 늘어난 셈이었다.

**예산 하나가 한도에서 차지하는 자리는 `max(총 예산, 쓴 돈) − 이미 잔고에 반영된 지출`이다.**
예산에 걸린 지출은 일반 지출 합계에서 **빠진다** — 그러지 않으면 예산으로 한 번, 지출로 또
한 번 세어진다. 그래서 예산 안에서 쓰는 한 한도는 움직이지 않고, **초과분만** 더 깎인다.
불변식은 `실제 사용액 + 남은 예약 = 총 예산` 이고 `domain/budget.test.ts` 가 못 박아 뒀다.

**예산에 걸 수 있는 지출은 기간 안의 비반복 항목뿐이다** (`inBudget`). 기간 밖이거나
반복이면 **일반 지출로 돌아간다** — 어디에도 안 세어지는 돈은 만들지 않는다. 반복을
막는 이유는 주머니 하나에 몇 번 들어갈지가 애매해지기 때문이다.

**예산을 지워도 연결된 지출은 남는다.** 그 돈들은 일반 지출로 돌아갈 뿐이다. 삭제
확인창이 그 사실을 먼저 말한다.

**세이브는 잔고를 건드리지 않는다.** 통장에는 그대로 있고, 쓸 수 있는 한도에서만 빠진다.
날짜가 없으므로 언제 보든 같은 금액이 묶여 있다.

**옛 값은 지우지 않는다.** `repay` · `priority` · `living` · `save` · `free` 는 지금까지와
똑같이 계산된다. 목록에서 지우면 `converters` 가 모르는 값을 `expense` 로 떨어뜨려 입금이
지출이 되고, 잔고에 영향이 없던 항목(`save` · `free`)이 잔고를 깎는다. 사라지는 것은 새
입력 화면에서 **고르는 자리**뿐이다 (`NEW_MONEY_TYPES`). 기존 `living` 을 예산으로 자동
변환하지도 않는다 — 실제 예산인지 단순 기간 지출인지 가릴 수 없다.

**옛 종류로 저장된 항목은 편집 화면에 '(예전)' 으로 보여 준다.** 안 보여 주면 이 항목이
지금 무엇으로 계산되고 있는지 알 수 없고, 다른 칸을 고치려다 종류가 조용히 바뀐다.

**사용처는 한 번에 하나만 걸린다.** `budgetId` 와 `debtId` 를 같이 걸면 어느 쪽에서 나간
돈인지가 두 개가 된다. 들어올 돈으로 바꾸면 둘 다 지운다 — 입금은 예산에서도 대출에서도
나가지 않고, 남겨 두면 예산이 그 입금을 지출로 세어 남은 금액이 늘어난다.

**`budgetId` 는 `linkedEntryId` 와 다른 관계다.** 그쪽은 "이 돈을 쓰게 만든 할 일" 이고
이쪽은 "이 돈이 어느 주머니에서 나가는가" 다. 재사용하지 않는다.

**달력 셀도 카드와 같은 예약을 본다.** 한쪽만 빼면 카드가 거절한 돈을 달력이 쓸 수 있는
것처럼 적는다.

**예산 지출을 세느라 화면용 목록을 tide 로 돌리지 않는다.** `materialize()` 결과를 넘기면
반복 항목이 한 번 더 전개돼 같은 입출금을 여러 번 센다. `virtual` 표식이 `TideInputError`
로 막지만, 애초에 두 목록을 섞지 않는 것이 규칙이다.

## 회복 (Recovery)

**휴식을 기록하는 기능이 아니라, 휴식을 빚지 않게 관리하는 기능이다.** 피로도·컨디션·
점수·활동량 분석은 이 기능의 것이 아니다. 있는 것은 간격 하나와 놓친 횟수 하나뿐이다.

**회복은 네 번째 축이 아니다.** 렌즈를 만들지 않는다. 회복 항목은 `kind: 'task'` 에
`recovery` 표식이 붙은 것이고, 그래서 캘린더·리스트·오늘 카드에 코드 변경 없이 얹힌다.

**미리 만들지 않는다.** 다음 날짜가 "마지막으로 실제로 완료한 날 + 간격" 이라 펼칠 수가
없다. 예정일은 규칙에 값 하나(`nextDueAt`)로 두고, 지평선(`generationHorizonDays`)에
들어섰을 때만 항목 한 건을 만든다. `recurrence` 를 쓰지 않는 이유가 이것이다.

**중복 생성을 막는 값은 `rule.activeEntryId` 다.** 항목 구독은 보고 있는 달 주변만
받으므로 "이미 만들었나" 를 항목 목록으로 판정하면, 먼 달을 열어 둔 사이에 같은 회차가
한 번 더 만들어진다. 규칙 문서는 달과 무관하게 항상 구독돼 있다.

**항목과 규칙은 한 배치로 쓴다** (`commitRecovery`). 이어 붙여 쓰면 — 오프라인 지속성
때문에 쓰기 promise 는 서버 ack 까지 resolve 하지 않는다 — 그 사이 새로고침 시 항목만
큐에 남고 `activeEntryId` 는 비어, 다음 접속이 같은 회차를 또 만든다.

**자동으로 도는 경로는 자기가 계산한 필드만 쓴다** (`patchRecoveryRule`). 설정 화면은
규칙 전체를 쓰므로, 예정일 채우기까지 전체를 쓰면 방금 적은 기본 메모를 덮는다.
merge 가 맵 안쪽까지 필드 단위로 합치는 것에 기대고 있다 (`rules.test.ts` 로 못 박아 뒀다).

**옮기기는 빚이 아니고, 건너뛰기는 빚이다.** 옮기면 새 시간이 실제로 잡혀 있으므로
`debtCount` 는 그대로다. 건너뛰면 +1 이고 항목은 사라진다 — 놓쳤다는 사실은 캘린더의
흔적이 아니라 빚 숫자가 기억한다. **회복을 삭제로 없앨 수 없다.** EntryModal 의 삭제도,
상세의 건너뛰기도 같은 확인창으로 모이고 빚을 남긴다.

**빚이 있는 동안 간격은 저절로 돌지 않는다.** `nextDueAt` 이 null 이 되고, "다시 잡기"
만이 다음 회차를 만든다. 빚이 0이 되는 순간의 완료일이 새 간격의 기준점이다 —
놓친 9/4 · 9/7 열을 복원하지 않는다. 목표는 달력 복원이 아니라 간격 재확보다.

**Rule 기본값과 회차 값은 다른 층이다.** 기본 메모·기본 옵션은 생성 시점에 **복사**된다.
회차에서 고쳐도 Rule 로 돌아가지 않고, 다음 회차는 다시 Rule 기본값에서 시작한다.

**회차는 옵션 label 을 스냅샷으로 들고 있다.** 설정에서 이름을 바꾸거나 지워도 지난
회차의 뜻이 사라지지 않는다. 상세도 켜져 있는 항목은 **스냅샷 이름**으로 보여 준다 —
나중 이름으로 덧칠하면 그때 무엇을 끄고 쉬었는지가 지워진다. 목록에서 지워진 항목은
칩을 채우지 않고 점선 윤곽으로 남긴다. 켜진 칩(`.on`)은 배경이 잉크색이라 점선 테두리가
그 위에서 보이지 않는다.

**OFF 항목 목록은 회복 상세에서 바로 고친다.** 사용자가 자기 기준(사우나 · 유튜브 …)을
손으로 쌓아 가는 목록이라, 새 항목을 적으려고 설정까지 가야 하면 결국 적지 않게 된다.
상세와 설정이 같은 조각(`RecoveryOptionManager`)을 쓰므로 갈래가 생기지 않는다.
상세에서 적은 항목은 목록에 더해지고 이 회차에서도 켜진다 (`addOptionFromEntry`) —
두 층을 한 번에 건드리는 유일한 동작이고, 다음 회차부터 기본으로 붙는 것이 의도다.

**같은 이름을 두 번 만들지 않는다.** 대소문자와 공백만 다른 이름은 같은 것으로 보고
(`findRecoveryOption`), 새로 만드는 대신 이미 있는 것을 켠다. 손으로 관리하는 목록이라
중복이 쌓이면 관리 자체가 일이 된다.

**빚이 0이면 회복 UI 는 화면에 없다.** 정상 상태에서 보이는 것은 회복 항목 1건뿐이고,
놓쳤을 때만 "회복 N회 밀림 · 다시 잡기" 한 줄이 뜬다. 대시보드를 만들지 않는다.

## 같이 보기 (SharedBoard)

**내 TODO 를 상대와 함께 보되, 그 화면에서의 수정이 원본으로 되돌아오지 않게 하는 기능이다.**

```text
내 TODO 원본
     ↓         (한 방향)
  같이 보기
```

**가르는 것은 누가 고쳤는지가 아니라 어느 화면에서 고쳤는지다.** 소유자가 공유 화면에서
고쳐도 원본은 그대로다. `domain/shared.ts` 에는 `Entry` 를 만들어 돌려주는 함수가 하나도
없고, `data/sharedRepo.ts` 에는 개인 경로에 쓰는 함수가 없다 — 역반영이 새어 나갈 자리가
코드에 없다. 규칙도 같은 방향을 막는다(상대는 `users/{owner}/entries` 를 읽지도 못한다).

**복제가 아니라 겹쳐 보기다.** 원본을 통째로 복사해 두고 매번 덮어쓰면 공유 화면에서
고친 값이 다음 갱신에 사라진다.

| 층 | 무엇 |
|----|------|
| `source` | 원본에서 마지막으로 받아 온 값 |
| `overrides` | 공유 화면에서만 고친 값 — **키가 있는 필드만** |
| 표시값 | `source ⊕ overrides` (`sharedView`) |

제목만 고쳐 둔 항목의 원본 날짜가 9/27 로 바뀌면 고친 제목과 새 날짜가 함께 보인다.

**고친 값이 원본과 같아지면 override 를 지운다.** 장식이 아니라 필수다 — 편집 화면은
제목·날짜·상태를 한 번에 제출하므로, 받은 필드를 전부 override 로 남기면 한 번 편집한
항목이 원본에서 통째로 떨어져 나간다. "고치지 않은 필드는 원본을 따라간다" 가 그 순간
거짓이 된다. 그래서 완료 체크를 풀면 상태 override 는 남지 않고 **지워진다**.

**갱신 쓰기는 `overrides` · `hidden` · `localOnly` 를 담지 않는다** (`sharedSourcePatch` +
merge). 기본값을 함께 보내면 상대가 감춰 둔 항목이 원본을 고칠 때마다 되살아난다.
그래서 **세 필드가 없는 문서가 정상이고**, 읽기 쪽이 `{}` · false 로 메우며 규칙도 있을
때만 형태를 본다. merge 가 실제로 무엇을 남기는지는 에뮬레이터에서만 확인된다
(`src/test/shared.emulator.test.ts`).

**공유 항목의 문서 id 는 원본 entry 의 id 와 같다.** 그래서 같은 항목이 두 번 만들어질
수 없고 갱신이 덮어쓰기 한 번으로 끝난다.

**원본을 올리는 것은 보드를 만든 사람뿐이다.** 같이 보기는 "내 TODO 를 상대에게 보여
주는" 자리다 — 초대받은 사람의 개인 TODO 는 올라가지 않고, 그쪽이 보드에 더하는 것은
이 화면에서 만든 항목(`localOnly`)뿐이다.

그 검사가 없으면 초대받은 사람의 TODO 도 올라갔다가, 소유자가 화면을 열 때 맞추기가
"원본이 없는 항목" 으로 보고 지운다 (맞추기는 소유자의 원본만 본다). 적은 것이 잠깐
보이다 말없이 사라지는 것이 이 앱에서 가장 하면 안 되는 일이다. 양쪽이 서로 올리게
하려면 맞추기도 `createdBy` 로 갈라야 하고, 그것은 아직 하지 않았다.

**화면은 누구의 TODO 가 오는지 갈라 적는다.** 초대받은 사람에게 "내 TODO 가 여기로
온다" 고 말하면 거짓이다 (수락 창과 빈 목록 안내 둘 다).

**원본 저장과 공유 갱신은 두 건의 쓰기다.** 한 배치로 묶으면 공유 쪽이 규칙에 걸리는
순간 개인 TODO 저장까지 함께 실패한다 — 공유 기능 때문에 기존 CRUD 가 멈추는 것은 어떤
정합성보다 나쁘다. 대신 어긋남을 오래 남기지 않는 장치를 둘 둔다.
- 실패한 공유 갱신은 다른 쓰기와 **같은** "저장하지 못한 것" 목록에 남는다 (`sharedSource`).
- 소유자가 화면을 열 때 **맞추기**가 남은 차이를 메운다 (`planOwnerSync`).

**맞추기는 원본 전량을 읽는다** (`fetchOwnerTasks`). 화면 구독 목록으로 판정하면 창 밖의
항목이 "원본이 사라졌다" 로 읽혀 통째로 지워진다. 공유 화면에서만 만든 항목
(`localOnly`)은 원본이 없는 것이 정상이라 지우지 않는다.

**색은 함께 간다.** 달력에서 항목을 가르는 값이라 원본의 색을 그대로 물려받는다 —
내 달력에서 빨강이던 일이 공유 화면에서 파랑이면 같은 일로 읽히지 않는다. 공유
화면에서 따로 고를 수도 있다(둘이 쓰는 기준이 내 기준과 다를 수 있다). 태그·장소·
금액은 가지 않는다.

모르는 색은 갈래가 다르다. `source` 쪽은 **기본색으로 떨어뜨리고**(보이는 값이라
거절하면 항목이 통째로 안 보인다), `overrides` 쪽은 **키를 버린다**(그 필드가 다시
원본을 따라간다). 뜻이 바뀌는 값(`status`)을 거절하는 것과 같은 기준이다.

**공유 대상은 `kind === 'task'` 뿐이다.** 아이디어와 가계부는 자동 공유하지 않는다.
회복 항목도 뺀다 — 휴식을 빚지 않게 관리하는 개인 시스템 항목이고, 캘린더에 올라온 한
건은 규칙이 방금 만든 것이다. 반복은 발생분으로 펼치지 않고 표식만 넘긴다(`recurring`).
할 일을 아이디어로 강등하면 공유에서 **지운다** — 어디에도 뜻이 없는 줄을 남기지 않는다.

**지나간 일정은 공유하지 않는다.** 같이 보기는 "둘이 앞으로 무엇을 하는가" 를 보는
자리다. 몇 년치 할 일이 통째로 올라가면 상대 화면이 지난 기록으로 덮여 이번 주에
무엇이 있는지 볼 수 없다 (실제로 230건이 올라갔다).

경계는 시작일이 아니라 **끝나는 날**이다 — 어제 시작해 모레 끝나는 일정은 아직 진행
중이므로 남고, 오늘 끝나는 것도 남는다. 반복은 시작일이 아무리 오래됐어도 지금 돌고
있으므로 `until` 로 판정한다 (끝이 없으면 계속, `until` 이 지났으면 뺀다). `count` 로
끝나는 반복은 마지막 회차를 알려면 전개해야 해서 그대로 둔다 — 남는 쪽이 지우는
쪽보다 낫다.

시간이 흐르기만 해도 어제 것이 지난 것이 되므로, **맞추기가 보드를 스스로 정리한다**
(`planOwnerSync`). 공유 화면에서만 만든 항목(`localOnly`)은 지난 날짜라도 지우지
않는다 — 원본이 없어 되살릴 곳이 없고, 그것은 보드의 자기 기록이다.

**원본을 지우면 공유 항목도 지운다.** tombstone 을 두지 않는다. 공유 화면에서 고쳐 둔
값도 함께 사라지므로, 삭제 확인창이 그 사실을 먼저 말한다. 반대로 **공유 화면의 삭제는
원본을 지우지 않는다** — 원본이 있는 항목은 삭제가 아니라 `hidden` 이고, 공유 전용
항목만 실제로 지운다.

**초대는 세 걸음이고, 링크만으로는 아무것도 열리지 않는다.**

```text
공유하기 → 보드 생성 + 초대장(100비트 난수) → 링크 전달
        → 상대 로그인 → 수락(자기 uid 하나 추가) → member
```

`sharedInvites/{code}` 는 로그인한 사용자가 **정확한 코드로 한 건만** 읽는다 (목록 조회는
소유자에게만 열려 있다 — 코드가 문서 id 라 목록 없이는 자기가 뿌린 링크를 되찾아 끊을
수도 없다). 수락은 보드에 자기 uid 하나를 더하는 것이고, 규칙이 그 코드의 초대장이 이
보드의 것인지 `get()` 으로 확인한다. 초대 코드는 `Math.random()` 으로 물러나지 않는다 —
암호학적 난수가 없으면 링크를 만들지 않는다.

**수락은 온라인이어야 한다.** 초대장을 읽어야 하고, 아직 member 가 아닌 상태의 쓰기는
서버 규칙만이 판정할 수 있다. 평소 편집과 조건이 다르므로 화면에서 미리 알린다.
**이미 들어와 있는 보드의 링크를 다시 누르면 쓰기를 보내지 않는다** — 규칙이 옳게
거절하지만 사용자에게는 고장으로 보인다.

**살아 있는 초대 링크는 보드당 하나다.** 새로 만들면 앞 링크는 끊어진다. 그러지 않으면
한 번 뿌린 주소가 영영 유효한 채로 남는다.

**활성 보드는 하나다.** 여러 개에 속해 있으면 **내가 만든 것**을 먼저 쓰고, 그 다음은
먼저 만든 순서다. 둘이 각자 '공유하기' 를 누른 뒤 링크를 주고받으면 서로 두 보드에
속하게 되는데, 그때 각자 자기 보드를 보는 것이 덜 놀랍다. 설정 창이 "참여 중인 보드가
N개" 라는 사실을 말해 준다 — 여러 보드를 관리하는 화면은 만들지 않는다.

**보드 내용은 화면이 열려 있을 때만 구독한다.** 보드 문서 하나는 늘 받는다 (그 값이
"TODO 저장을 공유에도 보낼지" 를 정한다). 항목·고정메모·D-Day 는 그 화면에서만 쓰이므로
닫혀 있는 동안 읽기를 사서 쓰지 않는다.

**고정메모는 개인 `Pin` 과 다른 자료다.** 뜻도(내 기준값 vs 둘의 공동 메모) 경로도 권한도
다르다. 연결하면 한쪽을 고칠 때 다른 쪽이 따라 바뀌어야 하는지가 매번 애매해진다.
보드당 **한 건**이고 문서 id 를 `'memo'` 로 고정한다 — 두 사람이 같은 순간에 처음 적어도
메모가 둘로 갈라지지 않는다. 메모 관리 시스템으로 키우지 않는다.

**D-Day 는 'D-23' 을 저장하지 않는다.** 그 문자열은 하루만 참이다. 저장하는 것은 제목과
날짜뿐이고 남은 날은 볼 때마다 센다 (`ddayCount`). 오늘은 `todayISO` prop 으로 받는다 —
화면이 시계를 직접 읽으면 자정을 넘긴 뒤 이 카드만 어제를 가리킨다. 순간에서 날짜를
뽑을 때는 `localDateOf()` 를 거친다. 개인 D-Day 위젯이 나중에 생겨도 **그것과 자동
동기화하지 않는다.** TODO 로 억지로 표현하지도 않는다 — D-Day 는 날짜 기준 정보이고
TODO 는 해야 할 행동이다.

**D-Day 는 앞의 두 건만 보이고 나머지는 접힌다.** 관리 화면을 따로 만들지 않는다.

**공유 화면도 달력이 기본이다.** 이 앱은 캘린더다 — 공유만 리스트 하나로 두면 항목이
쌓이는 순간 못 쓰게 된다. 달 이동과 캘린더·리스트 토글을 개인 화면과 **같은 조각**으로
두고, 달력은 `MonthCalendar` 를 그대로 쓴다 (월 그리드 · 기간 바 · "항목을 숨기지
않는다" 가 전부 거기 있고, 공유용으로 하나 더 그리면 두 달력이 서서히 달라진다).

공유 항목을 화면용 `Entry` 로 옮겨 넘긴다 (`asDisplayEntries`). 표시 전용이라 저장
경로에 닿지 않고, 돌아오는 길은 `onEntryClick` 의 id 하나뿐이다. **`virtual` 은 세우지
않는다** — 그 표식은 "반복을 펼친 사본" 이라는 뜻이고, 뜻이 다른 표식을 빌려 쓰면 다음
사람이 잘못 읽는다.

커서는 개인 화면과 **같은 값**이라 돌아가도 보고 있던 달에 있다. 보기 방식만 따로
기억한다 (`prefs.sharedView`) — 공유에서 리스트로 바꿨다고 내 TODO 까지 리스트가 되면
고친 적 없는 화면이 바뀐 것으로 보인다. 리스트는 보고 있는 달만 그리되 **날짜가 없는
항목은 늘 남긴다**; 달력에는 놓을 자리가 없으므로 빼고 몇 건이 빠졌는지 적는다.

**UI 에 관계를 박지 않는다.** `애인` 같은 말을 쓰지 않고 `같이 보기` · `TODO 공유하기` 로
적는다. 상대 표시명은 사용자가 실제로 연결한 계정에서 온다 (`displayName` 또는 이메일의
@ 앞). 같은 구조를 가족·친구와도 쓸 수 있어야 한다.

## 백업 · 복원

**백업 버전은 4다.** v4 부터 `budgets` · `reserves` 가 들어간다. 버전을 올린 이유는
읽는 쪽이 둘을 구분해야 하기 때문이다 — v3 파일에 두 배열이 없는 것은 정상(그 구조가
없던 시절)이고 v4 에 없는 것은 잘린 파일이다. 버전을 그대로 두면 그 둘을 가를 수 없어
어느 쪽이든 빈 배열로 넘겨짚게 된다. v2·v3 파일에 두 배열이 들어 있으면 **검증은 한다** —
손으로 고친 파일이 검사 없이 새어 들어가면 잘못된 예산이 한도를 조용히 깎는다.

**판정 순서는 app → version → 형식이다.** 예전에는 `items` 배열이 보이면 그 자리에서
이관 전 형식으로 넘겼고, 그래서 남의 앱 백업이나 아직 모르는 버전이 이관 변환기로 새어
들어갔다. 변환기는 방어적이라 거절하지 않고 무언가를 만들어 낸다. 메타데이터가 아예 없던
진짜 옛 백업(`items` 만 있는 파일)은 그대로 받는다.

**뜻이 바뀌는 값은 거절하고, 뜻이 흐려지는 보정은 보고한다.** `money.type` 과
`task.status` 는 허용 목록으로 막는다 — `converters` 는 읽기 계층이라 모르는 값을
`expense` · `planned` 로 조용히 바꾸는데, 그러면 입금이 지출이 되고 끝낸 일이 되살아난다.
색은 기본색으로 떨어질 뿐이라 거절하지 않고 `notes` 로 알린다.

**전체 교체를 못 하는 이유는 크기·분할·되돌리기다.** 클라이언트 SDK 에 컬렉션을 가로지르는
트랜잭션이 **없다는 것은 사실이 아니다** — `runTransaction` 이 그렇게 쓰이고 있다
(`createManyIfAbsent`). 진짜 이유는 트랜잭션 한 건이 문서 500개까지라는 것, 그보다 크면
여러 건으로 쪼개져 그 사이가 원자적이지 않다는 것, 덮이기 전 원본을 어디에도 굳혀 두지
않는다는 것이다. 대안으로 "손으로 먼저 지우라" 고 권하지 않는다 — 그것이 더 되돌릴 수 없다.

**병합은 회복 설정을 적용하지 않는다.** 회복 규칙은 컬렉션이 아니라 `users/{uid}` 문서의
필드라, 컬렉션 네 개만 쓰는 병합 경로가 건드리지 않는다. 파일에 회복 설정이 있으면
가져오기 전에 그 사실을 알린다 (`MERGE_SKIPS_RECOVERY`).

**가져오기 깃발은 파일 고르기 전에 세운다.** 파일 선택 창이 열려 있는 동안 한 번 더
누르면 두 번째도 통과해, 둘 다 서로의 중간 상태를 읽는다. 취소하거나 실패해도 반드시 내린다.

## 사실 정정 — 이전 CLAUDE.md 의 오류

이전 문서는 "비표준 doc ID(`__pins__`, `__balance__`)나 비표준 필드를 쓰면 보안 규칙에서
조용히 거부된다"고 적었고, 그래서 `title` 을 `'::balance::'` 로 쓰는 우회를 썼다.

**규칙은 doc ID 를 보지 않는다.** `__x__` 형태는 Firestore 예약어라 클라이언트 SDK
단계에서 `INVALID_ARGUMENT` 로 거부된다. 규칙을 아무리 열어도 통과하지 않는다.
에뮬레이터 테스트(`src/test/rules.test.ts`)로 확인했다. 따라서 그 우회는 처음부터
필요하지 않았고, 지금은 걷어냈다.

## 설계 원칙

- **기능 추가보다 제거를 우선 검토한다.** 방향은 계속 덜어내는 쪽이다.
- **가계부는 거래를 자동으로 가져오지 않는다.** 전부 수동 입력이다.
  본격 가계부 앱은 따로 쓰고, 여기서는 미래 현금 흐름만 눈으로 확인한다.
  단, 입력한 값으로 하는 **계산은 한다.** 예상 잔고를 사람이 암산할 이유가 없다.
- **캘린더는 항목을 숨기지 않는다.** "+N개 더" 대신 개수에 따라 바 높이를 압축해
  전부 표시한다 (4개 이하 일반, 5~7개 중간, 8개 이상 색상 띠만).
- **조용히 실패하지 않는다.** 건너뛴 항목은 이유와 함께 보고한다.
- **로컬 반영과 서버 확정은 다른 사건이다.** 오프라인 지속성 때문에 `setDoc` 은 로컬
  캐시를 먼저 갱신하고 `onSnapshot` 이 즉시 발화한다 — 화면에 보이는 것은 로컬 반영이다.
  쓰기 promise 는 서버 ack 까지 resolve 하지 않으므로 **기다려 화면을 막으면 안 된다**
  (오프라인에서는 영원히 대기한다).
- **거절당한 쓰기는 로컬에도 남지 않는다.** 에뮬레이터에서 재 봤다 — 로컬 캐시에 약 90ms
  보였다가 Firestore 가 되돌리고, 새로고침해도 돌아오지 않는다. 한때 "이 기기에서만 보이는
  값으로 남습니다" 라고 안내했는데 사실이 아니었고, 사용자는 적은 것을 통째로 잃었다.
  그래서 값을 **우리가** 붙잡는다 (`useWriteQueue` · `data/pendingWrites.ts`).
- **실패한 쓰기는 계정별 목록에 남는다.** `localStorage` 의 `calendarx.failed.<uid>` 라
  새로고침을 넘기고, 계정을 바꾸면 앞 계정 것이 보이지도 않고 보내지지도 않는다. 보관이
  막히면(사생활 보호 모드 등) "남겨 두었습니다" 라고 말하지 않는다.
- **실패는 확인창이 아니라 줄로 알린다.** 확인창은 두 건이 동시에 실패하면 뒤엣것이
  앞엣것을 밀어내고, 한 번 닫으면 다시 찾을 길이 없다. `FailedWrites` 는 남아 있는 동안
  계속 보이고 거기서 다시 보내거나 버린다.
- **옛 값이 새 값을 덮지 않는다.** 다시 보내기 전에 서버의 현재 `updatedAt` 과 견준다
  (`freshnessOf`). 더 새로운 저장이 있으면(`stale`) 또는 견줄 값이 없는 종류면
  (`unknown` — 순서 저장 · 회복 설정) 덮어쓸지 먼저 묻는다.
- **대기와 거절은 다르다. 가르는 것은 오류 코드가 아니라 promise 다.** 연결이 없어 아직
  못 보낸 쓰기는 **pending** 인 채로 남는다 — 오프라인 큐가 들고 있고 연결이 돌아오면
  스스로 나가므로 여기 걸리지도 않는다. 반대로 **거절된** 쓰기는 큐에서 이미 빠졌고
  아무도 다시 보내 주지 않는다. `unavailable` 이든 `permission-denied` 이든,
  `navigator.onLine` 이 무엇이든 마찬가지다. 한때 오류 코드를 보고 "연결되면 보냅니다" 로
  넘겼는데 그 쓰기는 실제로 다시 보내지지 않았고 적은 값도 함께 사라졌다.
  **거절은 종류를 가리지 않고 전부 붙잡는다.**
- **계정이 바뀐 뒤 도착한 실패도 버리지 않는다.** A 에서 저장을 걸어 두고 B 로 바꾼 사이에
  A 의 실패가 도착하는 순서가 실제로 있다. 그때 값은 **A 계정 자리에** 적고 B 화면에는
  띄우지 않는다. A 로 돌아오거나 새로고침하면 그대로 있다. 지나간 계정 갈래는 `setState`
  밖에서 처리한다 — updater 안에서 읽고-더하고-쓰면 StrictMode 의 이중 호출에 두 번 쌓인다.
- **가져오기는 온라인이어야 한다.** 이미 있는 문서를 덮지 않으려면 읽기와 쓰기를 한 번에
  해야 하고(`runTransaction`), 트랜잭션은 서버 없이 돌지 않는다. 평소 편집과 다른 조건이라
  선택 화면에서 미리 알린다. 오프라인이면 그 사실도 함께 적는다.
- **렌더가 무너져도 하얀 화면을 남기지 않는다.** `ErrorBoundary` 가 무엇이 났는지 적고
  다시 그려 보기와 새로고침을 준다. 오류를 0원이나 빈 목록으로 바꿔 그리지 않는다 —
  그러면 사용자가 잘못된 숫자를 사실로 읽는다.

## 개발

```bash
npm run dev            # 개발 서버
npm run check          # 타입 + 린트 + 테스트
npm run test:rules     # 에뮬레이터가 필요한 테스트 (자동 기동, Java 필요)
npm run emulators      # Auth + Firestore 에뮬레이터
```

`npm test` 는 에뮬레이터 없이 도는 것만 돌린다. `src/test/*.emulator.test.ts` 와
`rules.test.ts` 는 `npm run test:rules` 쪽이다 — 보안 규칙 · 복원 순서 · **규칙 거절 뒤에
무엇이 남는가** · **같이 보기의 merge 갱신이 상대의 수정을 남기는가**를 진짜 저장소 위에서
확인한다. `Promise.reject` 를 흉내 내는 테스트로는 규칙이 실제로 무엇을 거절하는지도,
거절 뒤 저장소에 무엇이 남는지도, merge 가 어느 필드를 남기는지도 알 수 없다.

규칙을 고쳤으면 `npm run test:rules` 를 돌린 뒤 배포한다.
**저장 계층(`repo.ts` · `restore.ts` · `sharedRepo.ts`)을 고쳤을 때도 마찬가지다** —
`writeMany` 가 실패를 결과로 돌려주는 모양과 `sharedSourcePatch` 가 상대의 `overrides` 를
남기는 모양은 에뮬레이터에서만 확인된다.

## 형제 앱

- **/tide (잔고캘린더)** — tide-over 저장소의 앱 **그 자체**다. 계산만 옮기고 화면은
  캘린더X 토큰으로 새로 그렸던 이식판이 있었는데, 그래서 '달력' 탭에 달력이 없었다 —
  월 그리드도, 날짜별 한도도, 기간 예산 띠도 원본에만 있었다. 지금은
  `src/pages/Tide/` 아래에 `lib/` · `components/` · `store.ts` · `styles.css` 를
  독립적으로 두고, 캘린더X 런타임 코드를 import 하지 않는다.
  계산 호환 테스트에서만 본앱 함수를 불러 같은 입력의 결과를 비교한다.

  **HTML 도 번들도 CSS 도 따로다** (`tide/index.html` + `src/pages/Tide/main.tsx`,
  vite `rollupOptions.input` 에 두 엔트리). 예전에는 앱 하나가 `location.pathname` 을
  보고 갈래를 정했는데, 배포 환경에서 정적 파일 · _redirects · 캐시 중 무엇이 먼저
  잡히느냐에 따라 /tide 에서 캘린더X 가 떴다. 지금은 두 앱이 서로를 대신 띄울 수 없고,
  잔고캘린더 번들에는 Firebase 도 Pretendard 도 들어가지 않는다.

  저장 키는 원본 그대로 `tideover.state` / `tideover.schema`, 백업 링크는 `#b=` 다.
  짧게 배포됐던 이식판의 `calendarx.tide.v1` 은 첫 로드 때 한 번 옮긴다
  (`lib/migrateLegacy.ts`). 옛 URL(`tide-over.stupidpoohh.workers.dev`)의 데이터는
  오리진이 달라 넘어오지 않는다 — 설정의 백업 링크로 옮겨야 한다. 옛 URL 자체는
  리다이렉트 워커가 여기로 넘겨 준다 (`docs/tide-over-worker/`).

  **`domain/tide.ts` 와 `pages/Tide/lib/calc.ts` 는 같은 규칙의 두 벌이다.** 전자는
  캘린더X 의 `Entry` 를, 후자는 원본의 `Entry` 를 본다. 한쪽 규칙을 고치면 다른 쪽도
  봐야 한다. 합치지 않은 이유는 데이터 모델이 다르고 두 앱이 따로 돌기 때문이다.

  **생활비·세이브도 /tide에 반영했다.** 로컬 State의 budgets/reserves와
  Entry.budgetId를 사용한다. 기간 안의 한 번짜리 출금만 예산에 연결되고,
  기간 밖·반복·입금·삭제된 예산 참조는 일반 입출금으로 계산한다.
  예산 삭제는 지출을 지우지 않는다. 한도는 본앱처럼
  max(예산, 사용액)에서 이미 잔고에 반영된 사용액을 뺀 금액을 예약한다.
  정산은 실제 입출금을 그대로 보고, 일반 예정 목록은 연결 지출을 중복 표시하지 않는다.

  **/tide 저장·백업은 v5다 (본앱 JSON 백업 v4와 별개).** v1~v4는 빈
  budgets/reserves를 추가하고 기존 기간 지출을 자동 변환하지 않는다.
  v5에 두 배열이 빠져 있으면 잘린 데이터로 거절한다. 이미 포함된 필드는 옛 버전에서도
  검증한다. 실존 날짜·안전한 정수 금액·컬렉션 내 중복 ID를 확인한 뒤 복원한다.
  지출 금액은 0 이상, 잔고는 음수 정수도 허용한다. 저장이 거절된 기존 원문은 보존한다.
- **clear-week** — 종이 주간 플래너. `entries` 컬렉션에 `kind === 'task'` 로 새 항목만
  오간다. clear-week 의 `CAL.entryDoc()` 이 이쪽 firestore.rules 요구사항을 이미 채운다.

## 남은 일

`docs/ASSESSMENT.md` 의 Phase 3 이후. 소셜 로그인, 계정 삭제, 이용약관·개인정보처리방침,
랜딩 페이지, PWA, 에러 추적, 요금제.

### P1 안정화에서 남긴 것 (후속 P1)

- **안전한 전체 교체.** 별도 스냅샷 컬렉션에 원본을 먼저 굳히고, 그것이 확인된 뒤에만
  교체하는 방식. 그때까지 교체는 계속 꺼 둔다.
- **계좌별 정산.** 지금은 계좌마다 기준일이 다르면 확정 차액을 내지 않는다. 계좌 단위로
  구간을 따로 잡으려면 잔고 기록을 이력으로 남겨야 한다.
- **여러 통화.** 환율을 다루지 않는 한 지원하지 않는다. 지금은 섞이면 계산을 멈춘다.
- **오류 추적.** `ErrorBoundary` 가 잡은 것을 지금은 콘솔에만 남긴다. 붙일 자리는 그 안에
  주석으로 표시해 뒀다.

### 같이 보기에서 남긴 것

- **여러 공유 보드.** 지금은 하나만 열고(내가 만든 것 우선), 설정 창이 "참여 중인 보드가
  N개" 라는 사실만 알린다. 고르는 화면은 만들지 않았다.
- **초대 코드 회전 · 만료.** 지금은 소유자가 손으로 끊거나 새로 만드는 것뿐이다.
- **member 를 내보내기.** 규칙은 소유자가 목록에서 빼는 것을 허용하지만 화면에 자리를
  두지 않았다. 지금 할 수 있는 것은 보드를 지우는 것(`공유 그만두기`)이다.
- **보드 이름 바꾸기.** 만들 때 정하고 그 뒤에는 고칠 자리가 없다.
- **공유 항목의 반복 표시.** 반복 할 일은 한 건으로 보이고 표식만 붙는다. 발생분마다
  따로 고치는 것은 범위 밖이다.
- **`count` 로 끝나는 반복의 정리.** 마지막 회차를 알려면 전개해야 해서 지금은 지나도
  공유에 남는다.
- **지난 항목 정리 시점.** 소유자가 같이 보기를 열 때만 돈다. 한동안 안 열면 어제 것이
  남아 있다.
- **양방향 공유.** 지금은 보드를 만든 사람의 TODO 만 올라간다. 둘이 서로의 TODO 를
  올리게 하려면 맞추기가 `createdBy` 로 자기 몫만 보도록 갈라야 한다.

### 출시 전 조건 (별도 후속 P1)

계정 삭제(가입한 사용자가 자기 데이터를 지울 수 있어야 한다), 이용약관·개인정보처리방침,
운영 배포 절차와 보안 규칙 배포 확인. 이번 P1 안정화 범위에 넣지 않았다.

## 커뮤니케이션

- 격식 있는 표준어, 입니다 경어체. 과한 높임과 인터넷 말투는 쓰지 않는다.
- 사과문이나 자기비판 없이 지적을 반영한 결과물을 낸다. 변론하지 않는다.
- 막연한 칭찬 대신 구체적 관찰로 답한다.
- 정리 요청("정리해줘", "통합해줘")에는 취사선택 없이 전부 살린다. 이견은 맨 끝에 별항으로.

## Calendar X 화면 규칙

월 이동·보기·필터·추가를 헤더에 통합한다. 공유는 보조 아이콘이며 렌즈가 아니다. 고정 메모와 회복은 얇은 utility strip에 두며 필요할 때 펼친다. 일정 블록은 기존 색상·높이·간격·테두리·글자 배치와 밀집도별 압축을 그대로 유지한다. +N으로 항목을 숨기지 않는다. 캘린더 전용 마감 스타일은 src/styles/calendar-design.css에 있다. /tide 디자인은 별도다.

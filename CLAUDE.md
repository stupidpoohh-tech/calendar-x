# 캘린더X

할 일과 아이디어와 돈을 같은 날짜 위에서 관리하는 캘린더 웹앱.
상용 캘린더의 기능 과잉을 덜어내고, 실제로 쓰는 동작만 남긴다.
이전 이름은 Dada Calendar. Firebase 프로젝트 ID(`dada-calendar-524ec`)와 백업 파일의
`app` 문자열은 호환을 위해 옛 이름을 유지한다.

## 이 앱이 답하려는 질문

1. 오늘 무엇을 해야 하는가
2. 떠오른 것을 어디에 적어 두는가
3. **다음 입금까지 이 돈으로 며칠 버티는가**

3번이 차별점이다. 투두이스트·노션·뱅크샐러드를 각각 이기려 하면 이길 수 없지만,
세 축이 하나의 타임라인을 공유하는 제품은 드물다.

원래는 "이 달 말 예상 잔고" 로 답했는데, 예측이 잘 맞지 않는 것을 사용자가 여러 번
경험했다. 그래서 형제 앱 tide-over 의 원칙을 흡수했다 — **예측하지 않는다.**
표시하는 숫자는 "예상 잔고" 가 아니라 "이 날까지 쓸 수 있는 한도" 다.

## 구조 — 하나의 타임라인, 네 개의 렌즈

**탭은 컬렉션이 아니라 필터다.** 모든 항목은 `entry` 하나이고 `kind` 로만 갈린다.

| 렌즈 | kind | 용도 |
|------|------|------|
| 전체 | (없음) | 세 축을 한 화면에. 오늘 패널 + 통합 캘린더 |
| TODO | `task` | 할 일. 기간·중요·긴급·상태 |
| 💡 | `idea` | 아이디어 단문 |
| 가계부 | `money` | 예정 입출금 + 잔고 + 대출 + 현금흐름 |

축 간 이동은 `kind` 필드 하나를 바꾸는 일이다 (`convertKind`).
아이디어 → 할 일 승격이 이 구조의 핵심 이점이다.

## 기술 스택

- Vite + React 18 + TypeScript (strict, `noUncheckedIndexedAccess`)
- Firebase Auth (이메일/비밀번호) + Firestore
- 배포: Cloudflare Pages. Firebase config 는 `VITE_FIREBASE_*` 환경변수
- 테스트: vitest (도메인) + `@firebase/rules-unit-testing` (보안 규칙)

## 파일 구조

```
src/domain/     순수 함수만. React 를 import 하지 않는다
  types.ts        도메인 타입
  constants.ts    COLORS, LENSES, MONEY_TYPES, STATUSES
  date.ts         날짜 유틸. 시간대 의존은 todayISO() 하나뿐
  entry.ts        생성 · 파생 필드 · kind 변환
  recurrence.ts   반복 전개 (저장하지 않는 가상 항목)
  tide.ts         "며칠 버티나" 계산. tide-over 흡수. 한도·정산·요약
  recovery.ts     회복. 간격·빚·회차 생성. 순수 상태 전이만 담는다
  money.ts        최소 단위 정수 <-> 표시 문자열
  filters.ts      렌즈 · 필터
src/data/       Firestore 접근
  firebase.ts     초기화. 오프라인 지속성 ON
  repo.ts         구독 · 쓰기 · 배치
  converters.ts   문서 <-> 도메인 (읽기는 전부 방어적으로)
  backup.ts       내보내기 (파일 형식 · 버전)
  backupFile.ts   파일 읽기. **변환 전에** 원시 JSON 을 검증한다
  restore.ts      복원 검증 · 실행 순서. 전체 교체는 꺼져 있다
  migrate.ts      이관 전 items -> 신규 스키마
src/ui/         화면 컴포넌트 (App: 렌즈 화면, TideBar: 며칠 버티나 카드,
                balanceEditor: 잔고 편집 상태 + 조각. 카드가 소유한다,
                Recovery*: 회복 상세 · 밀린 회복 줄 · 설정 그룹 ·
                OFF 항목 목록 관리(상세와 설정이 같은 조각을 쓴다))
src/pages/Tide/ /tide 잔고캘린더. tide-over 저장소의 앱을 화면째로 옮겨 왔다.
                lib/ · components/ · styles.css 전부 원본. 캘린더X 코드를 쓰지 않는다
src/app/        셸과 상태 훅
src/styles/     tokens.css (디자인 토큰) + app.css (전 컴포넌트 스타일)
firestore.rules 보안 규칙
```

## Firestore 데이터 구조

```
users/{uid}/entries/{id}    일정 · 아이디어 · 지출 통합
users/{uid}/accounts/{id}   잔고 (계좌 단위)
users/{uid}/debts/{id}      대출 1건 = 1문서
users/{uid}/pins/{id}       고정 메모
users/{uid}/items/{id}      이관 전 구조. 읽기 전용
users/{uid}                 문서 자체. migratedAt + recovery (회복 규칙 1개)
```

**구독은 세 갈래다** (`useStore`).

| 갈래 | 범위 | 쓰임 |
|------|------|------|
| 화면용 | 보고 있는 달 ±1 | 캘린더·리스트에 그릴 항목 |
| 계산용 | **오늘** −1 ~ +14달 | 한도·다음 입금일·정산 |
| 반복 | 전량 | 월 조회로 잡히지 않는다 |

**effect 는 셋으로 나눠 둔다.** 한 effect 에 묶으면 달을 넘길 때마다 계산 구독까지 끊고
다시 붙는다 — 커서와 무관한 값인데도 재구독이 돌고, 그때마다 빈 목록이 흘러 머리 숫자가
깜빡인다. 지금은 커서를 옮기면 화면용 구독만 다시 붙는다.

**계산 창은 뒤로 한 달뿐이다.** 잔고 기준일이 그보다 오래됐다면 그 사이의 **비반복**
입출금이 목록에 없다 (반복은 전량 구독하므로 빠지지 않는다). 그 상태의 정산 차액은
실제보다 크다. `settle()` 이 `complete: false` 로 알리고, 화면은 확정 금액 대신
"무엇이 빠졌는지" 를 말한 뒤 잔고만 갱신한다.

`entry` 의 파생 필드는 **손으로 채우지 않는다.** 항상 `withDerived()` 를 거친다.

- `ymSpan: YearMonth[]` — 항목이 걸친 모든 달. `array-contains-any` 로 월 단위 조회
- `isRecurring: boolean` — `recurrence` 에서 파생

## 지켜야 할 것

**한글 IME.** Enter 핸들러에는 반드시 `e.nativeEvent.isComposing` 체크가 있어야 한다.
없으면 조합 중 Enter 가 두 번 발화해 입력이 사라진다.

**날짜는 문자열.** 전부 `'YYYY-MM-DD'` / `'HH:mm'` 벽시계 값이다. UTC 인스턴트로
저장하지 않는다. 8월 20일 14시 회의는 어디서 보든 8월 20일 14시다.
패딩 없는 값(`2026-5-3`)이 섞이면 `normalizeDate()` 로 맞춘다.

**순간에서 날짜를 뽑을 때는 `localDateOf()` 를 쓴다.** `Date#toISOString().slice(0, 10)`
은 UTC 로 환산한 날짜라, 한국 시간 오전 9시 이전이면 하루가 밀린다. 테스트는
`vite.config.ts` 가 `TZ=Asia/Seoul` 로 못박아 돌린다 — UTC 기기에서는 이 부류의
결함이 아예 드러나지 않기 때문이다.

**오늘은 최초 렌더에 고정되지 않는다.** `useToday()` 가 자정 타이머 ·
`visibilitychange` · `focus` 세 갈래로 다시 잰다. 이 값 하나가 계산 창(`tideWindow`) ·
한도 · 정산 기준 · 오늘 카드를 전부 정하므로, 하루가 밀리면 화면 전체가 조용히 어제를
가리킨다. 화면 컴포넌트는 시계를 직접 읽지 않고 `todayISO` prop 으로 받는다.
값이 실제로 달라졌을 때만 상태를 바꾼다 — 리렌더가 나면 구독이 다시 붙는다.

**금액은 최소 단위 정수.** `amountMinor`. KRW 는 원 단위. 실수를 쓰지 않는다.

**tide 계산은 필터와 무관하다.** 필터로 항목을 가렸다고 한도가 늘어나면 안 된다.
그래서 `MonthCalendar` 는 그리는 목록(`entries`, 필터 적용됨)과 한도를 재는 목록
(`tideEntries`, 필터 이전)을 따로 받는다.

**가계부 렌즈 달력은 날짜마다 한도를 적는다.** 잔고캘린더가 셀마다 적어 주던
"이 날까지 쓸 수 있는 돈"이다 (`limitOn` · `compactAmount`). 오늘 이전에는 적지
않는다 — 지나간 발생분은 한도를 못 건드리므로 잔고만 반복될 뿐이다.

**과거 지출은 입력하지 않는다.** 잔고를 옮겨 적는 순간 그 사이의 변동 지출이
정산된 것으로 본다 (`domain/tide.ts` 의 `settle`). 잔고에는 `asOf`(날짜)와
`checkedAt`(ISO datetime) 을 함께 둔다.

**`asOf` 는 경계, `checkedAt` 은 순서다.** 정산 구간의 시작은 `asOf` 다 — 예정 항목은
`startDate` 만 갖고 시각이 없으므로 정산은 애초에 날짜 단위로만 정확할 수 있다.
`checkedAt` 은 하루에 두 번 적거나 계좌가 여럿일 때 어느 기록이 가장 최근인지 고르는
데만 쓴다. 여기서 날짜를 뽑아 쓰지 않는다.

**자료가 모자라면 확정 차액을 내지 않는다.** 기준일이 계산 구독 구간(`tideFrom`)보다
앞서면 그 사이의 한 번짜리 입출금이 목록에 없다. 그럴듯하게 틀린 숫자를 내느니
무엇이 빠졌는지 말하고 잔고만 갱신한다 (`Settlement.complete` · `coveredFrom`).

**한도의 끝점은 "다음 예정 입금 전날"이다.** 급여일 같은 별도 필드는 없다. 급여도
그냥 예정 입금이고, 주급(7일마다)도 같은 규칙으로 돈다. 다음 입금이 없으면 30일 뒤로
폴백한다. 기간 예산(span)은 흐름이라 "다음 입금" 에서 제외한다 — 안 그러면 끝점이
매일 내일이 된다.

**기간 예산은 표시(총액)와 계산(하루 몫)이 다르다.** 목록에는 총액 한 줄로 보이지만,
계산은 일할 몫이 기준이라 기간 중간에 잔고를 다시 적어도 정산이 어긋나지 않는다.
한도(`limitOn`)만 예외 — 기간에 들어서는 순간 남은 몫 전체를 예약한다.

**오늘 이전에 지나간 발생분은 한도를 못 건드린다.** 이미 잔고에 반영된 값이라
`limitOn` 은 `(오늘, d]` 만 더한다. 과거 항목을 지워도 한도가 변하지 않는 게 정상이다.

**화면용 목록과 계산용 원본은 다른 목록이다.** `materialize()` 는 반복 항목을 화면에
그릴 발생분으로 펼치는데, 펼친 사본에는 원본의 `recurrence` 가 그대로 남는다. 그 목록을
tide 에 넘기면 발생분마다 반복이 **한 번 더** 전개돼 같은 입출금을 여러 번 센다
(주간 1만원 한 달치가 −4만이 아니라 −14만이 된다). 그래서 펼친 사본은 `virtual` 표식을
달고, tide 의 모든 입구가 그것을 보고 `TideInputError` 로 거절한다. `virtual` 은 저장
경로에 없으므로 사용자 데이터로는 켤 수 없다 — 배선 실수만 잡힌다.

**계산 창은 커서가 아니라 오늘을 기준으로 잡는다.** 한도·다음 입금일·정산은 오늘의
함수이지 보고 있는 달의 함수가 아니다. 커서 창으로 계산하면 달력을 넘길 때마다 머리
숫자가 바뀐다. 앞으로 14달인 이유는 `horizonOf` 가 다음 입금을 400일까지 찾기 때문이다 —
창이 그보다 좁으면 실제로 있는 입금을 못 보고 "입금 없음 → 30일 뒤" 로 조용히 물러난다.
달력의 날짜별 한도는 계산 창이 덮는 달에만 적는다. 자료가 없는 달에 0을 더해 그럴듯하게
틀린 값을 내지 않는다.

**전체 교체는 꺼져 있다.** (`data/restore.ts` 의 `safeReplace`)

한때 "먼저 쓰고 나중에 지우면 안전하다" 고 적었다. **틀렸다.** 백업은 기존 id 위에
그대로 쓴다(upsert). 문서 A 를 파일 내용으로 덮어쓴 뒤 문서 B 쓰기가 실패하면, 삭제를
한 번도 부르지 않았어도 A 의 원래 내용은 이미 사라져 있다. 여러 배치는 원자적이지 않고,
클라이언트 SDK 에는 컬렉션을 가로지르는 트랜잭션이 없다. 메모리에 든 원본으로 되돌리는
것도 안전장치가 못 된다 — 탭이 닫히면 그 원본도 사라진다.

그래서 **불완전한 파괴적 기능을 남기지 않는다.** `safeReplace()` 는 `RestoreIO` 를
받지도 않으므로 쓰기·삭제가 새어 나갈 자리가 없다. 안전한 교체(원본을 별도 스냅샷에
먼저 굳히고 그것이 확인된 뒤에만 교체)는 후속 작업이다.

**병합은 실제 쓰기 시점에 존재를 확인한다.** `fetchAll` 로 후보를 좁히는 것은 읽기·쓰기를
줄이기 위한 것일 뿐이다. `fetchAll → 없는 id 고르기 → setDoc` 사이에 다른 탭이 같은 id 를
만들면 덮어쓴다. `createManyIfAbsent()` 가 트랜잭션 안에서 존재 확인과 쓰기를 함께 해
그 창을 닫는다. 이미 있으면 **충돌로 남기고 건드리지 않는다.**

`writeMany` · `createManyIfAbsent` 는 **실패를 던지지 않고 결과로 돌려준다.** 부르는 쪽이
`failed` 를 읽지 않으면 한 건도 저장되지 않아도 "가져왔습니다" 가 뜬다. 실제로 그랬다.
건수는 파일 총계와 맞아떨어져야 한다 — `created + skippedExisting + conflicts + failed`.

**검증은 변환 *전에* 원시 JSON 을 본다.** (`data/backupFile.ts`)
`converters.ts` 가 빠진 값을 오늘 날짜나 기본값으로 메우는 것은 **이미 저장된 문서를
화면에 띄우기 위한** 것이다. 그것을 먼저 돌리고 검증하면 이미 멀쩡해진 값만 본다 —
잘못된 날짜는 오늘로, 소수 금액은 잘려서, id 없는 항목은 사라진 뒤였다.
`readBackupFile()` 이 JSON 파싱 → 구조·버전 검증 → 항목 검증 → 그다음에야 변환한다.
없는 컬렉션을 빈 배열로 넘겨짚지 않는다 — v2·v3 는 네 배열이 모두 필수다.
예전 형식 변환이 제외하거나 자른 항목은 `notes` 로 돌려주고 화면에 보여 준다.

**반복 전개분은 저장하지 않는다.** id 가 `원본id@YYYY-MM-DD` 형태이고,
편집·삭제는 `baseIdOf()` 로 원본을 향한다.

**반복은 원점부터 세지 않고 조회 범위 근처로 건너뛴다.** 2025년에 시작한 매일 반복을
2026년 9월에서 보면 원점부터는 600칸이 넘게 헛돈다. 예전에는 그 헛도는 칸을
`MAX_RECURRENCE_OCCURRENCES`(400)가 먼저 잘라서 오래된 무기한 반복이 캘린더에서
통째로 사라졌다. 상한을 올리는 대신 `firstIndexOnOrAfter()` 로 건너뛰고, 상한은
**화면에 낼 개수**의 상한으로만 남는다. `count` 는 여전히 원점부터 센다.
`domain/tide.ts` 쪽은 처음부터 건너뛰기를 하고 있었다 — 그래서 예전에는 화면에
안 보이는 지출이 한도만 깎는 상태였다.

**글씨 크기는 rem 으로만 적는다.** `app.css` · `base.css` 의 `font-size` 는 전부 rem 이고
(16px 기준), 설정의 '글씨 크기' 가 `html` 의 font-size 한 줄을 바꿔 전부를 함께 키운다.
px 로 적으면 그 규칙만 설정을 무시한다. 여백·아이콘은 px 그대로라 함께 커지지 않으므로
폭은 ±12.5%(14/16/18px)로 묶어 둔다. 기본값 '자동' 은 `html` 에 font-size 를 두지 않는
것이고, 그래야 브라우저·OS 의 글씨 크기 설정을 따른다.
좁은 화면(≤480px)의 상단 줄만 예외다 — 렌즈 네 칸과 로그인 버튼이 한 줄에 들어가야 해서
`min()` 으로 상한을 둔다. `src/pages/Tide/styles.css` 는 별개 앱이라 이 규칙 밖이다.

**렌즈 강조색을 배경으로 쓸 때는 `--on-lens` 를 짝으로 쓴다.** 강조색은 다크에서
밝아지므로 흰 글자를 고정하면 다크 모드에서 흰 배경에 흰 글자가 된다.

**한 렌즈에 카드는 하나다.** 전체 렌즈는 오늘 카드, 가계부 렌즈는 며칠 버티나 카드.
대출·고정 메모는 그 카드 **안쪽에** 접힌 줄로 들어간다(`TideBar` 의 children). 카드를
여럿 세우면 모바일에서 캘린더가 화면 두 번 아래로 밀린다.

**금액은 카드에 하나만 적는다.** 잔고를 따로 적지 않는다 — 예정된 입출금이 없는 구간은
한도가 곧 잔고라 같은 숫자가 두 번 뜨고, 남은 날이 하루면 하루 몫까지 같아져 세 번 뜬다.
큰 숫자를 누르면 그 자리에서 잔고를 고치고(`useBalanceEditor`), 그 아래에는 금액 없이
"언제 적은 값인지"(`BalanceNote`)만 남긴다. 하루 몫은 남은 날이 2일 이상일 때만 적는다.

**떠 있는 표면은 `--bg-elev`.** 다크에서 `--bg`(#0a0a0a)는 페이지 `--bg-soft`(#141414)보다
어두워서, 모달에 쓰면 배경에 잠겨 투명하게 보인다.

## 회복 (Recovery)

**휴식을 기록하는 기능이 아니라, 휴식을 빚지 않게 관리하는 기능이다.** 피로도·컨디션·
점수·활동량 분석은 이 기능의 것이 아니다. 있는 것은 간격 하나와 놓친 횟수 하나뿐이다.

**회복은 네 번째 축이 아니다.** 렌즈를 만들지 않는다. 회복 항목은 `kind: 'task'` 에
`recovery` 표식이 붙은 것이고, 그래서 캘린더·리스트·오늘 카드에 코드 변경 없이 얹힌다.

**미리 만들지 않는다.** 다음 날짜가 "마지막으로 실제로 완료한 날 + 간격" 이라 펼칠 수가
없다. 예정일은 규칙에 값 하나(`nextDueAt`)로 두고, 지평선(`generationHorizonDays`)에
들어섰을 때만 항목 한 건을 만든다. `recurrence` 를 쓰지 않는 이유가 이것이다.

**중복 생성을 막는 값은 `rule.activeEntryId` 다.** 항목 구독은 보고 있는 달 주변만
받으므로 "이미 만들었나" 를 항목 목록으로 판정하면, 먼 달을 열어 둔 사이에 같은 회차가
한 번 더 만들어진다. 규칙 문서는 달과 무관하게 항상 구독돼 있다.

**항목과 규칙은 한 배치로 쓴다** (`commitRecovery`). 이어 붙여 쓰면 — 오프라인 지속성
때문에 쓰기 promise 는 서버 ack 까지 resolve 하지 않는다 — 그 사이 새로고침 시 항목만
큐에 남고 `activeEntryId` 는 비어, 다음 접속이 같은 회차를 또 만든다.

**자동으로 도는 경로는 자기가 계산한 필드만 쓴다** (`patchRecoveryRule`). 설정 화면은
규칙 전체를 쓰므로, 예정일 채우기까지 전체를 쓰면 방금 적은 기본 메모를 덮는다.
merge 가 맵 안쪽까지 필드 단위로 합치는 것에 기대고 있다 (`rules.test.ts` 로 못 박아 뒀다).

**옮기기는 빚이 아니고, 건너뛰기는 빚이다.** 옮기면 새 시간이 실제로 잡혀 있으므로
`debtCount` 는 그대로다. 건너뛰면 +1 이고 항목은 사라진다 — 놓쳤다는 사실은 캘린더의
흔적이 아니라 빚 숫자가 기억한다. **회복을 삭제로 없앨 수 없다.** EntryModal 의 삭제도,
상세의 건너뛰기도 같은 확인창으로 모이고 빚을 남긴다.

**빚이 있는 동안 간격은 저절로 돌지 않는다.** `nextDueAt` 이 null 이 되고, "다시 잡기"
만이 다음 회차를 만든다. 빚이 0이 되는 순간의 완료일이 새 간격의 기준점이다 —
놓친 9/4 · 9/7 열을 복원하지 않는다. 목표는 달력 복원이 아니라 간격 재확보다.

**Rule 기본값과 회차 값은 다른 층이다.** 기본 메모·기본 옵션은 생성 시점에 **복사**된다.
회차에서 고쳐도 Rule 로 돌아가지 않고, 다음 회차는 다시 Rule 기본값에서 시작한다.

**회차는 옵션 label 을 스냅샷으로 들고 있다.** 설정에서 이름을 바꾸거나 지워도 지난
회차의 뜻이 사라지지 않는다. 상세도 켜져 있는 항목은 **스냅샷 이름**으로 보여 준다 —
나중 이름으로 덧칠하면 그때 무엇을 끄고 쉬었는지가 지워진다. 목록에서 지워진 항목은
칩을 채우지 않고 점선 윤곽으로 남긴다. 켜진 칩(`.on`)은 배경이 잉크색이라 점선 테두리가
그 위에서 보이지 않는다.

**OFF 항목 목록은 회복 상세에서 바로 고친다.** 사용자가 자기 기준(사우나 · 유튜브 …)을
손으로 쌓아 가는 목록이라, 새 항목을 적으려고 설정까지 가야 하면 결국 적지 않게 된다.
상세와 설정이 같은 조각(`RecoveryOptionManager`)을 쓰므로 갈래가 생기지 않는다.
상세에서 적은 항목은 목록에 더해지고 이 회차에서도 켜진다 (`addOptionFromEntry`) —
두 층을 한 번에 건드리는 유일한 동작이고, 다음 회차부터 기본으로 붙는 것이 의도다.

**같은 이름을 두 번 만들지 않는다.** 대소문자와 공백만 다른 이름은 같은 것으로 보고
(`findRecoveryOption`), 새로 만드는 대신 이미 있는 것을 켠다. 손으로 관리하는 목록이라
중복이 쌓이면 관리 자체가 일이 된다.

**빚이 0이면 회복 UI 는 화면에 없다.** 정상 상태에서 보이는 것은 회복 항목 1건뿐이고,
놓쳤을 때만 "회복 N회 밀림 · 다시 잡기" 한 줄이 뜬다. 대시보드를 만들지 않는다.

## 사실 정정 — 이전 CLAUDE.md 의 오류

이전 문서는 "비표준 doc ID(`__pins__`, `__balance__`)나 비표준 필드를 쓰면 보안 규칙에서
조용히 거부된다"고 적었고, 그래서 `title` 을 `'::balance::'` 로 쓰는 우회를 썼다.

**규칙은 doc ID 를 보지 않는다.** `__x__` 형태는 Firestore 예약어라 클라이언트 SDK
단계에서 `INVALID_ARGUMENT` 로 거부된다. 규칙을 아무리 열어도 통과하지 않는다.
에뮬레이터 테스트(`src/test/rules.test.ts`)로 확인했다. 따라서 그 우회는 처음부터
필요하지 않았고, 지금은 걷어냈다.

## 설계 원칙

- **기능 추가보다 제거를 우선 검토한다.** 방향은 계속 덜어내는 쪽이다.
- **가계부는 거래를 자동으로 가져오지 않는다.** 전부 수동 입력이다.
  본격 가계부 앱은 따로 쓰고, 여기서는 미래 현금 흐름만 눈으로 확인한다.
  단, 입력한 값으로 하는 **계산은 한다.** 예상 잔고를 사람이 암산할 이유가 없다.
- **캘린더는 항목을 숨기지 않는다.** "+N개 더" 대신 개수에 따라 바 높이를 압축해
  전부 표시한다 (4개 이하 일반, 5~7개 중간, 8개 이상 색상 띠만).
- **조용히 실패하지 않는다.** 건너뛴 항목은 이유와 함께 보고한다.

## 개발

```bash
npm run dev            # 개발 서버
npm run check          # 타입 + 린트 + 테스트
npm run test:rules     # 에뮬레이터가 필요한 테스트 (자동 기동, Java 필요)
npm run emulators      # Auth + Firestore 에뮬레이터
```

`npm test` 는 에뮬레이터 없이 도는 것만 돌린다. `src/test/*.emulator.test.ts` 와
`rules.test.ts` 는 `npm run test:rules` 쪽이다 — 보안 규칙과 복원 순서를 진짜
저장소 위에서 확인한다.

규칙을 고쳤으면 `npm run test:rules` 를 돌린 뒤 배포한다.
**저장 계층(`repo.ts` · `restore.ts`)을 고쳤을 때도 마찬가지다** — `writeMany` 가
실패를 결과로 돌려주는 모양은 에뮬레이터에서만 확인된다.

## 형제 앱

- **/tide (잔고캘린더)** — tide-over 저장소의 앱 **그 자체**다. 계산만 옮기고 화면은
  캘린더X 토큰으로 새로 그렸던 이식판이 있었는데, 그래서 '달력' 탭에 달력이 없었다 —
  월 그리드도, 날짜별 한도도, 기간 예산 띠도 원본에만 있었다. 지금은
  `src/pages/Tide/` 아래에 `lib/` · `components/` · `store.ts` · `styles.css` 를
  원본 그대로 두고, 캘린더X 코드는 한 줄도 import 하지 않는다.

  **HTML 도 번들도 CSS 도 따로다** (`tide/index.html` + `src/pages/Tide/main.tsx`,
  vite `rollupOptions.input` 에 두 엔트리). 예전에는 앱 하나가 `location.pathname` 을
  보고 갈래를 정했는데, 배포 환경에서 정적 파일 · _redirects · 캐시 중 무엇이 먼저
  잡히느냐에 따라 /tide 에서 캘린더X 가 떴다. 지금은 두 앱이 서로를 대신 띄울 수 없고,
  잔고캘린더 번들에는 Firebase 도 Pretendard 도 들어가지 않는다.

  저장 키는 원본 그대로 `tideover.state` / `tideover.schema`, 백업 링크는 `#b=` 다.
  짧게 배포됐던 이식판의 `calendarx.tide.v1` 은 첫 로드 때 한 번 옮긴다
  (`lib/migrateLegacy.ts`). 옛 URL(`tide-over.stupidpoohh.workers.dev`)의 데이터는
  오리진이 달라 넘어오지 않는다 — 설정의 백업 링크로 옮겨야 한다. 옛 URL 자체는
  리다이렉트 워커가 여기로 넘겨 준다 (`docs/tide-over-worker/`).

  **`domain/tide.ts` 와 `pages/Tide/lib/calc.ts` 는 같은 규칙의 두 벌이다.** 전자는
  캘린더X 의 `Entry` 를, 후자는 원본의 `Entry` 를 본다. 한쪽 규칙을 고치면 다른 쪽도
  봐야 한다. 합치지 않은 이유는 데이터 모델이 다르고 두 앱이 따로 돌기 때문이다.
- **clear-week** — 종이 주간 플래너. `entries` 컬렉션에 `kind === 'task'` 로 새 항목만
  오간다. clear-week 의 `CAL.entryDoc()` 이 이쪽 firestore.rules 요구사항을 이미 채운다.

## 남은 일

`docs/ASSESSMENT.md` 의 Phase 3 이후. 소셜 로그인, 계정 삭제, 이용약관·개인정보처리방침,
랜딩 페이지, PWA, 에러 추적, 요금제.

## 커뮤니케이션

- 격식 있는 표준어, 입니다 경어체. 과한 높임과 인터넷 말투는 쓰지 않는다.
- 사과문이나 자기비판 없이 지적을 반영한 결과물을 낸다. 변론하지 않는다.
- 막연한 칭찬 대신 구체적 관찰로 답한다.
- 정리 요청("정리해줘", "통합해줘")에는 취사선택 없이 전부 살린다. 이견은 맨 끝에 별항으로.
